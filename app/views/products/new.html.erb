<% content_for :title, "New Product" %>

<div class="flex items-center gap-1.5 text-sm text-slate-400 mb-5">
  <%= link_to "Products", products_path, class: "hover:text-slate-600 transition-colors" %>
  <span>/</span>
  <span class="text-slate-600 font-medium">New</span>
</div>

<div class="max-w-lg">
  <h1 class="text-2xl font-bold text-slate-800 mb-5">New Product</h1>
  <div class="bg-white rounded-xl border border-slate-200 shadow-sm p-6">
    <%= render "form", form_object: @form %>
  </div>
</div>
